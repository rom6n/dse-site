const BackButton = () => {
  return (
    <a
      className="fixed flex items-center justify-center text-3xl text-white/90 font-inter bottom-4 right-4 items-cente w-15 h-15 backdrop-blur-sm rounded-full bg-black/60 z-50"
      href="#top"
    >
      ↑
    </a>
  );
};

export default BackButton;
